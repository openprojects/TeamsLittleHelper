<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
	android:layout_width="wrap_content" android:layout_height="fill_parent"
	android:orientation="vertical"
	android:background="@color/NiceButtonEndColor"
	>
	
	<include layout="@layout/header" 
		android:id="@+id/headerPlace"
        android:layout_height="wrap_content" 
        android:layout_width="fill_parent" />
        
	<ListView 
		android:id="@+id/roster" 
		android:layout_width="fill_parent"
		android:layout_height="0dip" 
		android:choiceMode="multipleChoice"
		android:background="@color/NiceButtonEndColor"
 		android:layout_below="@+id/headerPlace" 
		android:layout_above="@id/bottom_control_bar" android:cacheColorHint="@color/NiceButtonEndColor">
	</ListView>

	<LinearLayout android:id="@+id/bottom_control_bar"
		android:layout_width="fill_parent" 
		android:layout_height="wrap_content"
		android:layout_alignParentBottom="true" 
        android:layout_centerInParent="true"
		>
		<Button 
			android:id="@+id/add_player"
			android:text="@string/add_player"
			
			android:layout_width="fill_parent"  
			android:layout_height="wrap_content"
			android:layout_margin="10dp"
			android:layout_centerInParent="true"
			android:layout_alignParentTop="true"
			android:background="@drawable/nice_button"
			android:textColor="@android:color/white"
			android:textSize="18sp"
			android:shadowRadius="1"
			android:shadowDx="1"
			android:shadowDy="1"
			android:shadowColor="@android:color/black" 
		/>
	</LinearLayout>
</RelativeLayout>